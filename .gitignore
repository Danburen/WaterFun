.idea
*.iml

*.env
*.key
# deploy/ 目录 - 安全配置上 git，敏感文件单独排除
deploy/config/common-dev-secrets.yml
# deploy/docker/docker-compose.prod.yml — 从 .gitignore 移除，生产部署文件应有版本控制

node_modules
pnpm-lock.yaml
*.sql
!*.example.sql
!/sqls/*.sql

application.properties
application-*.properties
application-*.yml
docker-compose.yml

.gradle
.vscode
# /sqls/ — 从 .gitignore 移除，建表脚本应有版本控制

# java
*.jar
*.class
/waterfun-common-lib/build/

.omo
.ai/

# Docker dev data (bind mounts)
.data/

# ip2region xdb data files (downloaded by setup-ip2region scripts)
deploy/utils/*.xdb
/build
*.code-workspace