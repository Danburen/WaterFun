.idea
*.iml

# deploy/ 目录 - 安全配置上 git，敏感文件单独排除
*.env
*.key
deploy/config/common-dev-secrets.yml
.env.prod.secrets

node_modules
# pnpm-lock.yaml 已提交，不可忽略（CI 需要 frozen-lockfile）

# sql
*.sql
!*.example.sql
!/sqls/*.sql
!**/db/migration/*.sql
*.dump.sql

.gradle
.vscode
# /sqls/ — 从 .gitignore 移除，建表脚本应有版本控制

# java
*.jar
*.class
/waterfun-common-lib/build/

# agent
.omo
.ai/
.opencode
AGENTS.md

# Docker dev data (bind mounts)
.data/

# ip2region xdb data files (downloaded by setup-ip2region scripts)
deploy/utils/*.xdb
/build
*.code-workspace